# ======================================
# URSUS - Environment Configuration
# ======================================
# Copy this file to .env and fill in real values
# NEVER commit .env to version control!

# ======================================
# Stripe Configuration
# ======================================
# Get these from: https://dashboard.stripe.com/apikeys
# Use test keys (sk_test_) for testing, live keys (sk_live_) for production
STRIPE_SECRET_KEY=sk_live_your_platform_secret_key_here

# Get this from: https://dashboard.stripe.com/webhooks
# Create webhook endpoint pointing to your domain/webhook
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Connected Account ID (starts with acct_)
# From your Connected Account in Stripe Dashboard
CONNECTED_ACCOUNT_ID=acct_your_connected_account_id

# ======================================
# Security
# ======================================
# Generate a strong random API key (minimum 32 characters)
# Command: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
URSUS_API_KEY=your_secure_random_api_key_here

# ======================================
# Application Configuration
# ======================================
# Environment: development or production
FLASK_ENV=production

# Port (default: 4242)
# In production with Nginx, this is internal only
PORT=4242

# ======================================
# Optional: Custom Account Names
# ======================================
# These appear in logs and API responses
# Default: "Platform Account" and "Connected Account"
PLATFORM_NAME=Platform Account
CONNECTED_NAME=Connected Account

# ======================================
# Optional: Redis (for distributed rate limiting)
# ======================================
# Uncomment if using Redis for multi-server deployments
# REDIS_URL=redis://localhost:6379/0

# ======================================
# Optional: Database (for transaction logging)
# ======================================
# Uncomment if using PostgreSQL
# DATABASE_URL=postgresql://user:password@localhost:5432/ursus

# ======================================
# Optional: Monitoring
# ======================================
# Uncomment if using Sentry for error tracking
# SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
